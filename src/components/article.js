import React from "react";

const Article = ({ article }) => {
  return (
    <article className="Article">
      <p>{article.title}</p>
      <h1>aqui</h1>
    </article>
  );
};

export default Article;
